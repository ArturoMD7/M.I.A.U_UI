name: miauuic
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"  # Asegúrate de usar un rango de versión válido

# Dependencies specify other packages that your package needs in order to work.
dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  qr_flutter: ^4.1.0
  http: ^1.3.0
  path_provider: ^2.1.5
  provider: ^6.0.0
  shared_preferences: ^2.2.2
  intl: ^0.18.1
  flutter_dotenv: ^5.0.2
  url_launcher: ^6.2.5
  pdf: ^3.10.7
  printing: ^5.12.0

# Dependencies for development (testing, linting, etc.)
dev_dependencies:
  flutter_test:
    sdk: flutter
  cached_network_image: ^3.3.1
  flutter_pdfview: ^1.0.0+17
  barcode_scan2: ^4.5.0
  flutter_lints: ^5.0.0  # Esta es una dependencia de desarrollo, debe estar aquí

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - .env

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # To add custom fonts to your application, add a fonts section here:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic